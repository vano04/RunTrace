{
  "mcpServers": {
    "mono": {
      "title": "Mono",
      "description": "Retrieve prior experiment evidence and record agent research runs.",
      "command": "uvx",
      "args": [
        "--from",
        "mono-research[mcp]==0.1.6",
        "mono-mcp"
      ],
      "env_vars": ["MONO_BASE_URL", "MONO_API_TOKEN"]
    }
  }
}
