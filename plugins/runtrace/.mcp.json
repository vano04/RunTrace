{
  "mcpServers": {
    "runtrace": {
      "title": "RunTrace",
      "description": "Retrieve prior experiment evidence and record agent research runs.",
      "command": "uvx",
      "args": [
        "--from",
        "runtrace-ai[mcp]==0.1.3",
        "runtrace-mcp"
      ],
      "env_vars": ["RUNTRACE_BASE_URL", "RUNTRACE_API_TOKEN"]
    }
  }
}
